{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
    "workspaces": [
      "packages/nuxt",
      "packages/components",
      "packages/vue"
    ],
  "scripts": {
    "test": "jest",
    "dev": "yarn server && yarn web",
    "prod": "yarn server:prod && yarn web:prod",
    "web:dev": "cd packages/minute-web && yarn dev",
    "web:start": "cd packages/minute-web && yarn start",
    "web:generate": "cd packages/minute-web && yarn generate",
    "server:dev": "cd packages/newmont-graph && start:dev",
    "web:deploy": "cd packages/minute-web &&  ./deploy.sh",
    "server:serverless": "cd packages/minute-graph && yarn start:serverless",
    "server:prod": "cd packages/newmont-graph && start",
    "server:deploy-dev": "cd packages/newmont-graph && deploy:dev",
    "server:deploy-prod": "cd packages/newmont-graph && deploy:prod"
  },
  "dependencies": {
    "@nuxtjs/dotenv": "^1.4.1",
    "apollo-server-plugin-response-cache": "^0.5.3"
  }
}
